---
configuration:
  config_file:
    name: Filename of the config file
    description: The filename which is added to /shared/ for making out the location of the included configuration file.
  use_default:
    name: Use Default
    description: Whether to use the included configuration or not. Deactivate before restarting with adapted /share/<config>
network:
  443/tcp: HTTPS (SSL) Port
  80/tcp: HTTP (non-SSL) Port
